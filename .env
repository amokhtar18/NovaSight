# ============================================
# NovaSight Environment Configuration
# ============================================
# Copy this file to .env and customize values
# NEVER commit .env to version control

# ============================================
# Application Settings
# ============================================
APP_NAME=NovaSight
APP_ENV=development
DEBUG=true
LOG_LEVEL=DEBUG

# ============================================
# Test Credentials
# ============================================
TEST_USER_EMAIL=admin@novasight.dev
TEST_USER_PASSWORD=Admin123!

# ============================================
# Security Keys (CHANGE IN PRODUCTION!)
# ============================================
SECRET_KEY=novasight-dev-secret-key-change-in-production-32chars
JWT_SECRET_KEY=novasight-jwt-secret-key-change-in-production-32
CREDENTIAL_ENCRYPTION_KEY=novasight-encryption-key-32byte!

# ============================================
# PostgreSQL (Metadata Store)
# ============================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=novasight_platform
POSTGRES_USER=novasight
POSTGRES_PASSWORD=novasight
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================
# ClickHouse (Data Warehouse)
# ============================================
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=default
CLICKHOUSE_DATABASE=novasight

# ============================================
# Redis (Cache & Sessions)
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redistest123!
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/0

# ============================================
# Apache Airflow
# ============================================
AIRFLOW_HOST=airflow-webserver
AIRFLOW_PORT=8080
AIRFLOW_BASE_URL=http://${AIRFLOW_HOST}:${AIRFLOW_PORT}
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=airflow

# Airflow Database
AIRFLOW_POSTGRES_USER=airflow
AIRFLOW_POSTGRES_PASSWORD=airflow
AIRFLOW_POSTGRES_DB=airflow

# ============================================
# Apache Spark
# ============================================
SPARK_IMAGE=apache/spark:3.5.4
# Use Docker network hostname for local development
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077
SPARK_MASTER_URL=spark://${SPARK_MASTER_HOST}:${SPARK_MASTER_PORT}
SPARK_MASTER_WEBUI_PORT=8080
SPARK_MASTER_CONTAINER=novasight-spark-master
SPARK_WORKER_MEMORY=2G
SPARK_WORKER_CORES=2
# Execution mode: docker (local dev via REST API), ssh (remote), local (Spark client installed)
SPARK_EXECUTION_MODE=docker
# Spark REST Submission API URL (Standalone clusters)
SPARK_REST_URL=http://spark-master:6066
# Leave empty to use Docker/local execution instead of SSH
# For remote Spark servers, set to the SSH-accessible hostname
SPARK_SSH_HOST=

# ============================================
# Ollama (Local LLM)
# ============================================
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://${OLLAMA_HOST}:${OLLAMA_PORT}
OLLAMA_MODEL=qwen3-vl:8b

# ============================================
# Frontend
# ============================================
VITE_API_URL=http://localhost:5000
VITE_WS_URL=ws://localhost:5000

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# ============================================
# Pagination
# ============================================
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# ============================================
# File Upload
# ============================================
MAX_CONTENT_LENGTH=16777216
UPLOAD_FOLDER=/tmp/uploads

# ============================================
# Email (Optional - for notifications)
# ============================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@novasight.dev
SMTP_USE_TLS=true
