# NovaSight Backend Dependencies
# ===============================
# Python packages for Flask API
# Updated: January 2026

# Flask Core
Flask==3.1.2
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.7.1
Flask-CORS==5.0.1
Flask-Limiter==3.8.0
flask-restx==1.3.0

# Database
SQLAlchemy==2.0.46
psycopg2-binary==2.9.10
mysql-connector-python==9.1.0
oracledb==2.5.1
alembic==1.14.0

# Validation
pydantic==2.12.5
email-validator==2.2.0
marshmallow==3.23.1

# HTTP Client
httpx==0.28.1

# Redis (staying on 5.x for Python 3.9 compatibility)
redis==5.2.1

# Encryption
cryptography==46.0.4

# File formats (used by dlt file pipelines and openpyxl for Excel)
openpyxl>=3.1
pyarrow>=15
pandas>=2.0
s3fs>=2024.2.0
chardet>=5
clamd>=1.0.2
python-magic-bin; sys_platform == "win32"
python-magic; sys_platform != "win32"

# Password Hashing
argon2-cffi==23.1.0

# Template Engine
Jinja2==3.1.5

# WSGI Server
gunicorn==23.0.0

# ClickHouse
clickhouse-connect==0.10.0
clickhouse-driver==0.2.9

# AWS (for backup/recovery)
boto3==1.42.37
botocore==1.42.37

# Kubernetes (for backup job management)
kubernetes==31.0.0

# Utilities
python-dotenv==1.0.1
python-dateutil==2.9.0

# Logging
structlog==24.4.0

# Monitoring
prometheus-client==0.21.1

# Testing
pytest==8.3.4
pytest-flask==1.3.0
pytest-cov==6.0.0
factory-boy==3.3.1
faker==33.1.0

# Development
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.14.1

# ============================================
# Dagster - Orchestration
# ============================================
# Core Dagster (using 1.12.x for latest features, compatible with Python 3.11)
dagster==1.12.14
dagster-webserver==1.12.14
dagster-graphql==1.12.14

# Dagster Integrations (0.28.x matches dagster 1.12.x)
dagster-dbt==0.28.14
dagster-postgres==0.28.14

# GraphQL Client (for custom API calls)
gql[requests]>=3.5.0,<4.0.0

# Process manager for running multiple services
supervisor>=4.2.5

# ============================================
# dlt - Data Load Tool (Spark replacement)
# ============================================
dlt[clickhouse,filesystem,pyarrow,sql_database]==1.10.0

# ============================================
# Apache Iceberg - Data Lake Tables
# ============================================
pyiceberg[sql,s3fs]==0.9.0

# ============================================
# dbt DuckDB Adapter (for lake layer)
# ============================================
dbt-duckdb==1.9.1

# ============================================
# dbt ClickHouse Adapter (for warehouse / marts layer)
# ============================================
# NOTE: pin to 1.9.x to match dbt-core 1.9.10. This adapter is required by
# dbt/profiles.yml (all targets use type: clickhouse) — without it `dbt run`,
# `dbt debug`, etc. fail with "Could not find adapter type clickhouse!".
dbt-clickhouse==1.9.2
